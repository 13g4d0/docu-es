# Local-only artefacts (public repo: do not commit client ToR / binaries by default)
incoming/*.pdf
incoming/*.PDF
*.pem
.env
.env.*

# TdR full-text extract (run scripts/extract_tdr_pdf.py locally; do not redistribute without legal review)
docs/tor-gap/_extracted/

# MkDocs output
/site/

# Python venv for doc builds
.venv/
